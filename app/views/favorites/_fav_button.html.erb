

<%# あるポストをファボしているか？ %>
<% if current_user.fav?(micropost) %>

  <%= form_for(current_user.favorites.find_by(fav_id: micropost.id), html: { method: :delete }) do |f| %>
    <%= hidden_field_tag :fav_id, micropost.id %>
    <%= hidden_field_tag :params_id, params[:id] %>
    <%= hidden_field_tag :favd_id, favd_id %>
    
    <%= f.submit 'Unfav', class: 'btn btn-danger btn-xs' %>
  <% end %>
<% else %>
  <%= form_for(current_user.favorites.build) do |f| %>
    
    <%# フォローの場合showでは%>
    <%# ボタンに@userをuserとして渡している %>
    <%# users_con showアクション%>
    <%# @user = User.find(params[:id]) %>
    
    <%# User.find(params[:id]) → @user → user %>
    
    <%#  あるmicropostのidを取得してfav_idとしてcontrollerに渡す%>
    <%= hidden_field_tag :fav_id, micropost.id %>
    <%= hidden_field_tag :favd_id, favd_id %>
    
    <%= f.submit 'Fav', class: 'btn btn-primary btn-xs' %>
    
  <% end %>
<% end %>
